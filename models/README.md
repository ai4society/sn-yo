1. To add another language, include additional rows in the CSV with the same subject and label but a different language code.
2. Keep core.owl, base-sn.owl, variant01.owl, variant02.owl, variant03.owl, and SN_YO_labels_combined.csv in the same directory.
3. Create a virtual environment and install rdflib.
4. Run the Python script to read the CSV and add rdfs:label to each subject.
5. Save the updated OWL files generated by the script.
6. Merge all updated ontologies into master.owl for final use and querying.
